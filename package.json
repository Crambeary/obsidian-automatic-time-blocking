{
  "name": "obsidian-atb",
  "version": "0.0.1",
  "description": "Pulling tasks from the tasks plugin, and automatically assings them times of the day based off rules set by you.",
  "main": "lib/main.js",
  "license": "MIT",
  "scripts": {
    "build": "obsidian-plugin build src/main.ts --esbuild-config=esbuild.config.json",
    "dev": "obsidian-plugin dev src/main.ts --esbuild-config=esbuild.config.json"
  },
  "devDependencies": {
    "obsidian": "obsidianmd/obsidian-api",
    "obsidian-plugin-cli": "^0.4.3",
    "typescript": "^4.1.5"
  },
  "dependencies": {
    "node-ical": "^0.18.0"
  }
}
